Go to Inspect element
Go to Console
Paste:
crypto.subtle.digest('SHA-256', new TextEncoder().encode("GLAPFLAP")).then(b => console.log(Array.from(new Uint8Array(b)).map(x => x.toString(16).padStart(2, '0')).join('')));

Change GLAPFLAP to the password you want